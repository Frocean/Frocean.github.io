baseURL = "https://frocean.github.io/"
locale = "zh-CN"
title = "Frocean 的个人网站"

buildFuture = true

[params]
  description = "Frocean 的个人网站，基于 Hugo 重构"

[taxonomies]
  tag = "tags"
  category = "categories"

[markup]
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4
    ordered = false

  [markup.goldmark]
    [markup.goldmark.extensions]
      # 启用删除线扩展（允许使用 ~~text~~ 进行删除线）。
      # 我们在渲染前对数学区块内的 ~ 做特殊处理，避免冲突。
      strikethrough = true
      [markup.goldmark.extensions.passthrough]
        enable = false       # 通过 enable 子项控制开关
    [markup.goldmark.renderer]
      unsafe = true   # 允许在 Markdown 中直接使用 HTML（谨慎使用）
      hardWraps = false  # 如果希望回车换行就是 <br>，设为 true，一般保持 false 即可
    [markup.goldmark.parser]
      autoHeadingID = true
      attribute = true    # 允许使用 {.class} 语法

  [markup.highlight]
    style = "monokai"     # 代码块配色方案
    noClasses = false
